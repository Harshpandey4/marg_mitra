enum SafetyLevel {
  safe,
  caution,
  warning,
  critical,
}

enum FactorType {
  health,
  motion,
  environment,
}

enum DrivingCondition {
  normal,
  adverse,
  emergency,
}

enum WeatherImpact {
  clear,
  rain,
  snow,
  fog,
  storm,
}